import React from "react";

const Padding4 = ({children}:{children:React.ReactNode}) =>{

    return(
        <div className="p-4">
            {children}
        </div>
    )
}

export default Padding4